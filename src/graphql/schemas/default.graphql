type Query {
	_empty: String
}

type Mutation {
	_empty: String
}

scalar Upload
scalar Date

input Paginate {
	page: Int
	perPage: Int
}

enum SortOrder {
	ASC
	DESC
}

input Sort {
	sortField: String
	sortOrder: SortOrder
}
